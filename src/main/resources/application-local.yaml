---
spring:
    datasource:
        main:
            embedded: false
            jdbcUrl: jdbc:postgresql://localhost:5432/postgresql?user=postgres&password=postgresql
            username: postgres
            password: postgresql
        login:
            driver-class-name: org.h2.Driver
            embedded: true
    flyway:
        baseline-on-migrate: true

security:
    authorization:
        enabled: false
